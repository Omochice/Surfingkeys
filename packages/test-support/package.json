{
  "name": "@sk/test-support",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./helpers": "./src/helpers.ts",
    "./setup": "./src/setup.ts"
  },
  "scripts": {
    "check": "npm-run-all2 --parallel --aggregate-output check:*",
    "check:oxfmt": "oxfmt --check",
    "check:oxlint": "oxlint",
    "check:package": "sort-package-json --check",
    "check:type": "tsc --noEmit -p tsconfig.json",
    "fmt": "npm-run-all2 --parallel --aggregate-output fmt:*",
    "fmt:oxfmt": "oxfmt",
    "fmt:package": "sort-package-json"
  },
  "dependencies": {
    "dompurify": "catalog:",
    "vitest": "catalog:"
  },
  "devDependencies": {
    "@sk/types": "workspace:*",
    "npm-run-all2": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "sort-package-json": "catalog:",
    "typescript": "catalog:"
  }
}
