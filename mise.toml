[tools]
# Not in package.json: typos is not published to npm.
# Not the bare `typos`: that resolves to the cargo backend and builds from
# source, whereas aqua pulls a prebuilt binary.
# Not node/pnpm too: pnpm already owns those (devEngines, executionEnv.nodeVersion).
"aqua:crate-ci/typos" = "1.47.2"
